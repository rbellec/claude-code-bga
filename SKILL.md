# BGA Alpha — Claude Code Skill File

<!-- PLACEHOLDER — Replace this content with the actual SKILL.md from:
     ~/.claude/skills/bga-alpha/SKILL.md

     This file is the core artifact of the experiment: it encodes all
     BGA-specific knowledge that Claude Code needs to work effectively
     within the BGA Studio environment.

     Structure expected:
     - Phase 1: Project Setup (scaffold, Makefile, directory structure)
     - Phase 2: Implementation (Game.php, state classes, Game.js patterns)
     - Phase 3: Test Loop (deploy, create table, hotseat, read errors)
     - Common Errors table (with root causes and fixes)
     - Framework-specific pitfalls (SQL comments, table name conflicts, etc.)

     The skill file should be self-contained: an AI agent reading only this
     file should be able to set up, implement, and test a BGA game.
-->
