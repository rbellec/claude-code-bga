# Phase 3 — UI (Client Rendering + Interaction)

<!-- PLACEHOLDER — Insert the actual prompt(s) used for the JS client and CSS.

     ## Context
     PHP backend is deployed and (mostly) working. Now need the client
     to render the board, handle clicks, and react to notifications.

     ## The prompt
     (Paste the actual prompt text here)

     For QTTT, this included:
     - Board HTML: 3x3 grid with square IDs
     - Rendering: classical marks (large) vs spooky marks (small, multiple per square)
     - Interaction: two-click placement (select first square, then second)
     - State handlers: PlayerTurn (click squares), CollapseChoice (action buttons)
     - Notifications: spookyPlaced, cycleDetected, boardCollapsed, gameResult
     - CSS: grid layout, color coding (X=red, O=blue), hover/selection states

     ## Expected outcome
     - Game.js with state handlers and notification processing
     - CSS with board grid, mark styling, interaction states
     - Board renders correctly on page load
     - Clicks produce the expected actions
     - State transitions update the UI

     ## Notes
     - BGA's new framework uses ES6 modules (export class Game)
     - The constructor receives a `bga` object, not `gameui`
     - State handlers are registered via bga.states.register()
     - Assets reference: g_gamethemeurl + 'img/file.svg'
     - The test loop (deploy → reload → verify) is critical here
-->
