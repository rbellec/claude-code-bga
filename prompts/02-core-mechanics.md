# Phase 2 — Core Game Mechanics

<!-- PLACEHOLDER — Insert the actual prompt(s) used for game logic implementation.

     This is typically the most game-specific prompt. For Quantum Tic-Tac-Toe,
     it covered graph theory (cycle detection, collapse propagation). For other
     games, it would cover different mechanics.

     ## Context
     Config files and Makefile are working. Game.php and state class stubs exist.
     Now implementing the actual game logic.

     ## The prompt
     (Paste the actual prompt text here)

     For QTTT, this included:
     - State machine: PlayerTurn → CollapseChoice → CheckVictory → ComputeScores
     - Entanglement graph: adjacency list from uncollapsed moves
     - Cycle detection: DFS path finding before move insertion
     - Collapse options: two directions along the cycle path
     - Cascade collapse: iterative resolution of forced moves
     - Victory check: three-in-a-line with simultaneous-win tiebreaker

     ## Expected outcome
     - Game.php with graph helpers, collapse logic, victory detection
     - 4 state classes (PlayerTurn, CollapseChoice, CheckVictory, ComputeScores)
     - All PHP files pass lint
     - Successful deploy to BGA Studio

     ## Notes
     - This is where most bugs will surface during testing
     - The prompt should include the full rules, not a summary
     - Consider providing the rules in the original academic notation if available
-->
