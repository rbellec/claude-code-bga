# Phase 1 — Project Setup

<!-- PLACEHOLDER — Insert the actual prompt(s) used for initial setup.

     This prompt should cover:

     ## Context
     First interaction with Claude Code for a new BGA game project.
     The BGA Studio account exists, the game is registered, SFTP works.

     ## The prompt
     (Paste the actual prompt text here)

     Typically includes:
     - Game name and BGA username
     - Instruction to download the scaffold via SFTP
     - Create the local directory structure
     - Write gameinfos.inc.php, dbmodel.sql, stats.json, gameoptions.json
     - Create the Makefile with check + deploy targets
     - First `make deploy` to verify connectivity

     ## Expected outcome
     - All config files created and deployed
     - `make check` passes (PHP lint)
     - `make deploy` succeeds (files visible on BGA Studio)
     - Initial git commit

     ## Notes
     - The SKILL.md should already be in ~/.claude/skills/bga-alpha/
     - The prompt may reference specific game rules for DB schema design
     - BGA Studio SSH key propagation can take up to 1 hour
-->
